// Require the React framework
var React = require('react');

var GraphMain = React.createClass({
  render: function() {
    return (
      <div>

      </div>
    );
  }
});

module.exports = GraphMain;
